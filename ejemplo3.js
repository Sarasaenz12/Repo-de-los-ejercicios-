let ocupacioCancha = true;
let nombreCancha = "Bernabeu";

let mensaje = ocupacioCancha ? `la cancha llamada ${nombreCancha} esta ocupada"` : 
`la cancha llamda ${nombreCancha} esta disponible"`; 
console.log(mensaje);
